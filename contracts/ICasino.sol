// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

interface ICasino {

    event OwnerAdded(address account);
    event OwnerRemoved(address account);

    event DealerAdded(address account);
    event DealerRemoved(address account);

}